# OLLAMA_NETWORK: lan | wg | auto (auto tries lan first, then wg)
OLLAMA_NETWORK=auto
OLLAMA_LAN_URL=http://host-on-lan:11434
OLLAMA_WG_URL=http://host-on-wireguard:11434
OLLAMA_HERMES3_MODEL=hermes3:8b
OLLAMA_LLAMA3_MODEL=llama3:8b
OLLAMA_INPUT_COST_PER_1K_USD=0
OLLAMA_OUTPUT_COST_PER_1K_USD=0

# Students worked on at once. Leave at 1 unless the Ollama host can actually
# serve concurrent requests (GPU + OLLAMA_NUM_PARALLEL>1) -- on CPU it measured
# 0.96x, i.e. no gain. See GPU_SETUP.md.
REPORT_WORKERS=1

# Keeps the model and its KV cache resident between batches.
OLLAMA_KEEP_ALIVE=30m
# The prompt runs ~4,300 tokens; below 8192 Ollama silently truncates context
# rather than erroring.
OLLAMA_NUM_CTX=8192
OLLAMA_NUM_PREDICT=4096
OLLAMA_TEMPERATURE=0.3
